<section>
    <ol>
        {{#each notifications}}
        <li>
        <a href="/notification/{{this._id}}">{{this.title}}</a>
        <br>
        Descreption: {{this.event_type}}
        </li>
       {{/each}}
    </ol>
</section>